import React from 'react';
import styles from './Hero.scss';

function Hero() {
    return (
      <header className ={styles.component}>
        <h2 className={styles.title}>Things to do</h2>
        <img className={styles.title} src></img>
      </header>
    )
  }

export default Hero;
